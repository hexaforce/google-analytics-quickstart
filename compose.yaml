services:

  mariadb:
    image: mariadb:10.10
    command: '--default-authentication-plugin=mysql_native_password'
    restart: always
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'mysqladmin ping -h 127.0.0.1 --password="$$(cat /run/secrets/db-password)" --silent'
        ]
      interval: 3s
      retries: 5
      start_period: 30s
    secrets:
      - db-password
    environment:
      - MYSQL_DATABASE=analytics
      - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/db-password
    ports:
      - 3306:3306
      - 33060:33060
    # expose:
    #   - 3306
    #   - 33060
    networks:
      - backnet
    volumes:
      - db-data:/var/lib/mysql

  gunicorn-flask:
    build: gunicorn-flask
    restart: always
    ports:
      - '8000:8000'
    # expose:
    #   - 8000
    depends_on:
      mariadb:
        condition: service_healthy
    networks:
      - backnet
      - frontnet
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl --silent --fail localhost:8000/flask-health-check || exit 1"
        ]
      interval: 10s
      timeout: 10s
      retries: 3
    command: gunicorn -w 3 -t 60 -b 0.0.0.0:8000 app:app

  nginx-react:
    build: nginx-react
    restart: always
    volumes:
      - ./nginx-react/default.conf:/tmp/default.conf
    environment:
      - FLASK_SERVER_ADDR=gunicorn-flask:8000
    ports:
      - "80:80"
    depends_on:
      gunicorn-flask:
        condition: service_healthy
    networks:
      - frontnet
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl --silent --fail localhost:80/health-check || exit 1"
        ]
      interval: 10s
      timeout: 10s
      retries: 3
    command: /app/start.sh

volumes:
  db-data:


secrets:
  db-password:
    file: db-password.txt

networks:
  backnet:
  frontnet:
